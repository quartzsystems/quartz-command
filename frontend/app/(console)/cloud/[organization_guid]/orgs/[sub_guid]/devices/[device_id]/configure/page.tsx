"use client";

import { CloudSection } from "@/components/CloudSection";

export default function DeviceConfigurePage() {
  return (
    <CloudSection
      title="Configure"
      blurb="Pick a section on the left — Interfaces, NAT, and Firewall mirror this QuartzFire's local UI."
    />
  );
}
