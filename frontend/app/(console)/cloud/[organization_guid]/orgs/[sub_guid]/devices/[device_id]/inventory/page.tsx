"use client";

import { CloudSection } from "@/components/CloudSection";

export default function DeviceInventoryPage() {
  return (
    <CloudSection
      title="Inventory"
      blurb="Hardware details, certificate, and enrollment history for this QuartzFire will live here."
    />
  );
}
