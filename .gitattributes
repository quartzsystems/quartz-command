# Files executed or parsed on Linux must stay LF even on Windows checkouts
# (a CRLF shebang breaks the script, and dpkg/rpm scriptlets are strict).
*.sh text eol=lf
*.spec text eol=lf
*.service text eol=lf
scripts/packaging/deb/* text eol=lf
scripts/packaging/env/* text eol=lf
